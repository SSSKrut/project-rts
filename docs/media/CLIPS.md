# Медиа для README

Всё в этой папке снято с воспроизводимых сцен: та же команда — тот же кадр.
Пересобрать набор: `scripts/demo_clips.sh` (клипы) и `scripts/demo_stills.sh`
(стоп-кадры); один клип — `scripts/demo_clips.sh <имя>`.

| Папка | Что | В git |
|---|---|---|
| `gif/` | клипы под README (800–960 px, 15 fps, 8–12 с) | да |
| `png/` | постеры клипов + отдельные стоп-кадры 1920×1080 | да |
| `mp4/` | мастера клипов 1280×720 30 fps, crf 18 | нет (`.gitignore`) |

GIF с движущейся камерой весит в разы больше статичного: терраин зашумлён, и
любой сдвиг камеры меняет каждый пиксель. Поэтому камера в клипах либо стоит,
либо «режет» между планами (`-rec-track-snap`), а не едет. Если нужен именно
плавный полёт — берите mp4: GitHub показывает видео прямо в README, если
перетащить файл в веб-редактор (он ложится в `user-attachments`, ссылка
вставляется в markdown как есть).

## Клипы

| Клип | Что видно | Сцена |
|---|---|---|
| `bounding` | отделение идёт перебежками под огнём трёх пулемётчиков: половина бежит, половина прикрывает | `ai_bounding` |
| `clear_building` | зачистка дома: стек у двери, вход парой, прочёс этажа; крыша прозрачна — камера смотрит сверху | `ai_clear_building` |
| `focus_fire` | сосредоточение огня по названной цели, трассеры и снятие HP | `ai_focus_fire` |
| `vehicle_combat` | танк + БТР против БМП + ПТ-носителя: наводчики сами обнаруживают, разворачивают башни, стреляют | `ai_vehicle_combat` |
| `ambush` | засада: залёгшее отделение подпускает колонну БМП на 40 м и бьёт во фланг | `lite_balance_ambush` |
| `open_field` | обратная картина: три БМП накатывают на отделение в чистом поле | `lite_balance_open` |
| `convoy_road` | колонна БМП + 2 грузовика идёт по шоссе через мост, темп по самому медленному | `ai_vehicle_convoy_road` |
| `shellfire` | артналёт по позиции: отделение уходит из зоны разрывов и возвращается на рубеж | `ai_shellfire` |
| `march_column` | марш в колонне, строй держится на рельефе | `ai_march_column` |
| `air_cas` | ударный вертолёт назначен на корпус: захват по радару, пуск, отход | `ai_air_cas` |
| `city_aerial` | город 480×480 м: 268 зданий, сетка улиц, река с 7 мостами; обе стороны идут к точкам (4×, медленный облёт) | миссия `city` |
| `city_street` | та же миссия с уровня улицы у центральной точки, когда стороны сходятся | миссия `city` |
| `ui_command` | полный интерфейс: поле, инспектор, карта, панель отделения; отделение получает маршрут к точке | миссия `crossroads` |
| `ui_map` | раскладка «карта крупно»: маркеры обеих сторон и маршруты по городу | миссия `city` |

## Стоп-кадры (`png/`)

Постер каждого клипа лежит под именем клипа. Отдельно сняты:
`city_aerial_evening` (город в вечернем свете), `city_bridge` (мост через реку),
`mass_columns` (16 отрядов и 12 машин выходят со стартовой линии),
`tank_closeup` (модель танка крупно), `convoy_bridge` (колонна на мосту).

`main_menu` снят не стендом, а своим харнессом стартового экрана —
`go build -o bin/space ./cmd/space` и затем
`./bin/space -w=1920 -h=1080 -shot=main_menu.png -shot-at=420`. Кадр 420
берётся потому, что к нему догорел fade-in и погасла подсказка «not wired
up yet»; raylib кладёт PNG в рабочую папку, оттуда его переносят в `png/`.

## Как снят каждый клип

Команды — в `scripts/demo_clips.sh`; флаги камеры и записи описаны в
`.claude/CLAUDE.md` (раздел Clip capture). Кратко: `-scene`/`-mission` выбирает
сцену, `-rec-speed` — скорость времени, `-shot-cam=радиус,тангаж,азимут` —
стартовую орбиту, `-rec-track` — за кем следить, `-rec-clean` — без панелей.

## Размеры

| Клип | Длина GIF | GIF | mp4 |
|---|---|---|---|
| `air_cas` | 10 с | 1.1 МБ | 1.6 МБ |
| `ambush` | 10 с | 3.3 МБ | 4.5 МБ |
| `bounding` | 10 с | 2.0 МБ | 2.4 МБ |
| `city_aerial` | 12 с | 2.2 МБ | 2.6 МБ |
| `city_orbit` | 5 с | 4.7 МБ | 10.0 МБ |
| `city_street` | 10 с | 2.4 МБ | 1.0 МБ |
| `clear_building` | 10 с | 1.2 МБ | 1.7 МБ |
| `convoy_road` | 10 с | 2.1 МБ | 2.7 МБ |
| `focus_fire` | 8 с | 1.4 МБ | 1.4 МБ |
| `march_column` | 8 с | 1.5 МБ | 2.1 МБ |
| `open_field` | 10 с | 1.7 МБ | 1.7 МБ |
| `shellfire` | 10 с | 1.4 МБ | 1.6 МБ |
| `ui_command` | 10 с | 4.6 МБ | 4.1 МБ |
| `ui_map` | 12 с | 4.5 МБ | 2.5 МБ |
| `vehicle_combat` | 10 с | 1.7 МБ | 1.1 МБ |

В `png/`: `air_cas`, `ambush`, `bounding`, `city_aerial`, `city_aerial_evening`, `city_bridge`, `city_orbit`, `city_street`, `clear_building`, `convoy_bridge`, `convoy_road`, `convoy_sunset`, `focus_fire`, `main_menu`, `march_column`, `mass_columns`, `open_field`, `shellfire`, `tank_closeup`, `ui_command`, `ui_map`, `vehicle_combat`.
