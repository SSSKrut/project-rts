# План работ

Шесть блоков. Порядок — по зависимостям, а не по желанию. Каждый блок: что уже
стоит, что делаем, по чему считаем закрытым.

Легенда: ✅ работает · 🟡 частично · ❌ нет кода · 💀 код есть, читателя нет.

---

## Что уже стоит (срез 2026-08-25)

Это фундамент, который пивот НЕ трогает. Он больше, чем кажется, и в этом
главная причина, почему lite реализуем.

| Область | Статус |
|---|---|
| Мир: чанки, рельеф, реки, дороги лентами, здания, окопы, пропы, стриминг, сохранение | ✅ |
| Навигация: A* по мультиграфу, этажи и переходы, стили пути, ORCA, микропуть | ✅ |
| Сенсоры: каналы, дальности, falloff, маскировка, терраин-LOS, туман войны, ESM-пеленги | ✅ |
| Бой: рейкаст, классы, секторная броня, подавление, сплэш, три механизма выстрела | ✅ |
| Угроза: DangerBuffer → Threat, кластеры направлений, небезопасные зоны | ✅ |
| Реактивное поведение: инстинкт выживания, стойки, перебежки волнами, рефлексы техники и борта | ✅ |
| Техника: спеки, драйвер, дороги, уклонение, конвои, ПВО, GoDark | ✅ |
| Авиация: полёт, дилы, расписание, обнаружение, удар с воздуха, ракеты, рефлексы | ✅ |
| Приказы: сущность, очередь, жизненный цикл, история, таймлайн | ✅ |
| Стоячие правила: движение, огонь, поведение, доктрины, автономия | ✅ |
| Интерфейс: панели, инспектор, карта, лента событий, squad bar, превью, кольца охвата | ✅ |
| Стенд: 59 сцен с вердиктами, три гейта (детерминизм, эталон, save/load) | ✅ |
| **Связь** | 💀 `RadioNetwork` пишется, не читается |
| **Точки захвата** | ❌ |
| **Сценарий и победа** | ❌ |
| **Бот** | ❌ |
| Строй как объект управления | ✅ — **под снос** |
| Занятие зданий тремя приказами + интерьерные слоты | ✅ — **под снос** |
| Редактор символики | ✅ — **под снос** |
| `UtilityEvaluator` | 💀 8 режимов, 2 читателя — см. блок F |

---

## Блок A — Связь

**Зачем первым.** Это ядро. Всё остальное — либо снос, либо надстройка над ним.
Пока связи нет, бот и сценарий строить не на чем.

Делаем:
- `CommsState` на отряде + `Radio{On, EmitRangeM}` на радисте + `Relay{RangeM}`
  на точке и штабной машине. `RadioNetwork` поглощается.
- Пересчёт качества раз в ~1 с, полосы по таблице `MODEL.md` §1.
- Гейт доставки приказа в `SquadService.IssueOrder`: мгновенно / с задержкой /
  не доходит и висит «не доставлено».
- Радиус самостоятельности: отряд без связи не уходит дальше `Leash` от
  `Anchor`. Единственная новая строчка в поведении — остальные четыре пункта
  уже написаны.
- Гейт чужих засечек: `AwareDirect` уже отличает свою засечку от общей —
  добавить проверку полосы.
- Устаревание своих отрядов на карте при обрыве.
- Рация в проход излучателей: пеленг по работающей сети.
- Класс техники «штабная машина».
- UI: полоса связи в инспекторе отряда и на групповом боксе squad bar,
  «не доставлено» в строке приказа, кольца ретрансляторов на карте.

**Закрыто, когда:** сцена `lite_comms` меряет четыре утверждения — приказ
доходит в «Связи» и не доходит в «Обрыве»; смерть радиста роняет полосу на две;
отход к точке её возвращает; вражеский ESM даёт пеленг на работающую рацию и
теряет его после выключения.

---

## Блок B — Точки захвата и здания

Делаем:
- `ControlPoint` + система захвата присутствием (§2 модели).
- `BuildingControl` от того, кто внутри футпринта.
- Один приказ «Занять» вместо трёх; сносим `ClearSeq`, планировщик интерьерных
  слотов и матчер (~850 строк).
- Точка как ретранслятор — стык с блоком A.
- Отрисовка точек на карте и в 3D: владелец, прогресс, оспаривание.

**Закрыто, когда:** сцена `lite_capture` — нейтральная точка берётся одиночкой
за ~10 с и тройкой за ~3 с, обе стороны внутри замораживают прогресс, потеря
точки гасит ретранслятор и роняет связь соседнего отряда в тот же тик.

---

## Блок C — Снос строя и символики

Делаем:
- Сносим редактор формации, пресеты, кастомные слоты, ориентацию по северу,
  F1-F4 (~1000 строк UI и компонентов).
- Оставляем неявную россыпь для пехоты и колонну для техники, выбор
  автоматический.
- Сносим редактор символики (175 строк); символы на карте остаются.

**Закрыто, когда:** сюита сцен движения зелёная без изменений в поведении, а
отряд глазами читается как отряд. Второе проверяется плейтестом — если не
читается, чиним обводкой группы, а не возвратом строя.

---

## Блок D — Сценарий и победа

**Здесь игра впервые становится игрой.** Раньше этого блока проверять нечего.

Делаем:
- Манифест миссии (§7 модели), загрузчик, расписание сил на землю по образцу
  `AirArrival`.
- Условие победы на точках, таймер, экран итога с числами.
- Две-три реальные миссии на существующих картах.

**Закрыто, когда:** миссия проходится от начала до конца обеими исходами, а
итог читается без объяснений.

**Главный риск проекта.** «Условие победы» звучит просто, но это первое место,
где станет видно, что миссия скучная. Закладывать время на переделку.

---

## Блок E — Бот

Делаем:
- Оперативный ИИ на трёх глаголах (§8 модели), такт 10-30 с.
- Приказы через ту же радио-трубу, что у игрока.
- Оценка точек, распределение отрядов, ответ на потерю.

**Закрыто, когда:** бот берёт точки, отвечает на контратаку и **проигрывает,
если у него выбить радистов и ретрансляторы** — последнее и есть доказательство,
что механика связи симметрична.

---

## Блок F — Ревизия чисел

Идёт фоном, не блокирует. Разбирается на независимые куски:
- Решить судьбу `UtilityEvaluator`: дать режимам читателей или ужать до
  работающего (гейт огня + стойка + ярлык). До блока E — иначе бот встанет
  поверх слоя, про который непонятно, живой он.
- Убрать мёртвые поля: `Standoff`, `FireOnStruct`, `Occupancy` как публичный
  ledger, `OrderStateBlocked` (либо завести писателя, либо убрать из UI).
- Свести баланс в спек-таблицы, ничего не добавляя механизмами.
- Обновить `CLAUDE.md` под снесённые подсистемы.

---

## Порядок и почему он такой

```
A (связь) ──┬── B (точки, здания) ──┬── D (сценарий) ── E (бот)
            └── C (снос)  ──────────┘
                        F (числа) — фоном, F.1 обязателен до E
```

A первым, потому что это ядро. B и C можно вести параллельно: они не пересекаются
по коду и оба уменьшают поверхность. D невозможен раньше B — победа считается по
точкам. E последним, потому что боту нужны и точки, и связь, и сценарий.

**После E проект законченный.** Дальше — только числа, миссии и полировка.
